# ProfileRemote


## Development Workflow

```bash
cd ProfileRemote

# Start Re.Pack dev server
pnpm start

# In another terminal, launch Android
pnpm android

# Or launch iOS
pnpm ios

# Emit production JS bundles (host + remotes)
pnpm android:build

# Build signed Android APK 
pnpm build:apk`

```
